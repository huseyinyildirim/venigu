{% extends 'site/templates/app.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <div class="card-header">
                        Giriş Yap
                    </div>
                    <div class="card-body">
                        <form action="{{ current_path() }}" method="post" autocomplete="off" class="needs-validation" novalidate>
                            <div class="form-group">
                                <label for="mail">E-posta Adresiniz</label>
                                <input type="mail" name="mail" id="mail" class="form-control{{ errors.mail ? ' is-invalid':'' }}" placeholder="siz@alanadi.com" value="{{ old.mail }}" minlength="5" maxlength="320" required>
                                <div class="invalid-feedback">{{ errors.mail ? errors.mail | first:'E-posta adresi giriniz.' }}</div>
                            </div>
                            <div class="form-group">
                                <label for="password">Password</label>
                                <input type="password" name="password" id="password" class="form-control{{ errors.password ? ' is-invalid':'' }}" minlength="1" required>
                                <div class="invalid-feedback">{{ errors.password ? errors.password | first:'Şifre giriniz.' }}</div>
                            </div>

                            <button type="submit" class="btn btn-primary">Giriş Yap</button>

                            {{ csrf.field | raw }}
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block inlineStyles %}
    <link rel="stylesheet" href="{{ base_url() }}/assets/css/site/login/index.css">
{% endblock %}

{% block inlineScripts %}
    <script type="text/javascript" src="{{ base_url() }}/assets/js/site/login/index.js"></script>
{% endblock %}